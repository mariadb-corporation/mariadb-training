[mariadb]
#### PRIMARY SERVER CONFIGURATION
#server_id            = 1
#log_basename         = primary1
#log-bin              = /var/log/mariadb/primary1-bin
#log-bin-index        = /var/log/mariadb/primary1-bin.index
#binlog_format        = ROW
#expire_logs_days     = 10

#### REPLICA 1 SERVER CONFIGURATION
#read-only
#server_id            = 2
#log_basename         = replica1
#log-bin              = /var/log/mariadb/replica1-bin
#log-bin-index        = /var/log/mariadb/replica1-bin.index
#binlog_format        = ROW
#expire_logs_days     = 10

#### REPLICA 2 SERVER CONFIGURATION
#read-only
#server_id            = 3
#log_basename         = replica2
#log-bin              = /var/log/mariadb/replica2-bin
#log-bin-index        = /var/log/mariadb/replica2-bin.index
#binlog_format        = ROW
#expire_logs_days     = 10
